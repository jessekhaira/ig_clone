{
  "name": "IG_Clone",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "12.18.0"
  },
  "scripts": {
    "start": "cd client && npm install && npm run build && cd .. && cd backend && npm install && node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build && cd .. && cd backend && npm install"
  }
}
